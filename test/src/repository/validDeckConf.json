{
    _format_version: "1.1",
    _info: {
      select_tags: [apiName],
    },
    services: [
      {
        host: dnsParts[1],
        name: apiName,
        port: (dnsParts[0]=="http") ? '80' : '443',
        protocol: dnsParts[0],
        read_timeout: 60000,
        retries: 5,
        read_timeout: 60000,
        routes: createRoutesFromSpec(jsonSpecfile),
      },
    ],
}